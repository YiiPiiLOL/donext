# DoNext V2.11.10 — Android build

V2.11.10 keeps the Capacitor Android foundation from earlier releases. Android Studio/SDK is required to produce an APK.

```bash
npm install
npx cap add android
npm run build:android
```

This package does not contain a generated APK. The current build environment is not an Android SDK/Gradle release environment.
