import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'se.chiips.donext',
  appName: 'DoNext',
  webDir: 'public',
  server: { androidScheme: 'https' }
};
export default config;
